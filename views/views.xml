<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="ostool.list">
      <field name="name">ostool list</field>
      <field name="model">ostool.ostool</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="ostool.action_window">
      <field name="name">ostool window</field>
      <field name="res_model">ostool.ostool</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="ostool.action_server">
      <field name="name">ostool server</field>
      <field name="model_id" ref="model_ostool_ostool"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="ostool" id="ostool.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="ostool.menu_1" parent="ostool.menu_root"/>
    <menuitem name="Menu 2" id="ostool.menu_2" parent="ostool.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="ostool.menu_1_list" parent="ostool.menu_1"
              action="ostool.action_window"/>
    <menuitem name="Server to list" id="ostool" parent="ostool.menu_2"
              action="ostool.action_server"/>
    -->
  </data>
</odoo>